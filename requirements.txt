# requirements.txt - Optimized for Render's 512MB free tier

# Core web framework (choose ONE)
fastapi
uvicorn
python-multipart

# Environment & utilities
python-dotenv
requests

# PDF processing (lightweight)
pypdf

# LangChain (minimal)
langchain
langchain-community
langchain-text-splitters
langchain-groq

# Embeddings (CPU-only, lightweight)
sentence-transformers
transformers
torch==2.1.0+cpu -f https://download.pytorch.org/whl/torch_stable.html

# Hugging Face
huggingface-hub

# Vector database (CPU-only)
faiss-cpu

# Math
numpy

# Google OAuth
google-auth
google-auth-oauthlib
google-auth-httplib2

# Optional: if you need arxiv
arxiv
